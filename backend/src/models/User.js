/* ============================================================
   SoulGPT — User Model Re-export
   File: backend/src/models/User.js
   ============================================================ */

   const { User } = require('./index');

   module.exports = User;